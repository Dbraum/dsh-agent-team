# Team 多 Web 页面支持

## 状态

**已归档（2026-09-17）。** 第一阶段传输修复已实施、独立验收并合并进 master（PR #25 → `d7007d3`，并入本地 master `796ccba`）；Host `changes` 与 Client 改用 Harness 流式 Remote，多个同源页面可同时使用。实施分支 `fix/multi-web-stream` 与 `fix/multi-web-stream-only` 已删除（删除前全部 ref 已用 bundle 备份）。

**最后核查：2026-09-17（验收基线 `796ccba`）。** 产品代码、测试和文档已随 PR #25 入库；未修改用户 profile 或相邻 Harness 产品代码。**未随本工作项交付的宽验收项在下方「明确关闭的残余」逐条记录，本次归档不宣称完整多页面产品支持已完成。**

## 目标与范围

同一 Host 下多个 Team 页面能够同时使用，不因通知订阅阻塞普通加载。系统检查导航、草稿、未读和页面生命周期，区分“修复连接阻塞”与“完整多页面支持”；不把后一目标全部塞进第一项修复。

- [计划与待定决策](plan.md)：支持范围、方案取舍、状态归属、实施顺序和验收矩阵。
- [诊断与代码依据](materials/diagnosis.md)：复现结果、证据限制、源码导航、现有 Harness 能力。
- [01 — 多页面加载不再被更新订阅阻塞](issues/01-stream-invalidation.md)：首个完整实施任务。

## 进展与结论

- 任务 01 的传输修复已实施并通过独立验收（基线 `796ccba`）：`npm test` 636 通过 / 1 跳过；`npm run build` 通过（含客户端 bundle 纯度门禁）；`npm run test:browser` 两项通过（真实 Web 全流程 32.7s、四同源页面并发 13.0s，成员查询 3–8 ms）；`audit-ui-parity` 与 `lint` 为 0。master CI run 35186303253 双 lane 绿（linux + windows）。
- 回归用例有判别力：把维护中的四页面用例跑在旧实现源码上（保留新测试脚本）为红（第二页 probe 3 秒超时），在新实现上为绿 ⇒ 它确实覆盖原故障，不是空转。
- 验收中发现并修掉一个真缺陷：干净流结束被当成终止性失败，页面会永久不再接收更新（违反 Harness 的 carrier 契约）。修复 `b4915cf` 按 `accepted` 分流为 carrier 错误，并在 `connection/reset` 时重开失败的订阅；真浏览器 2×2 复核——未修版连断网重连都救不回来，修好后自动恢复。真机重启不会留下聋页面（两种关停顺序最终都落到该恢复路径）。
- 已删除旧长轮询、Team 自有退避和按旧版本过滤补读的 `ScopeCoverage`。Host 只保留一个待通知版本；每次开场基线触发补读；最后一位订阅者离开即取消流。
- 桌面与 390×844 截图已检查，留在忽略的 `artifacts/browser/`；界面没有新增控件。
- 原有测试环境的 WeakMap 渲染错误未处理：同一组 Agent 测试在改动前后各出现 48 次，真实浏览器没有对应页面异常。

## 明确关闭的残余（不随本工作项交付）

Human 2026-09-17 指示：把文件改到与现状一致后归档。以下各项**没有完成，也没有另立工作项**；列在这里是为了让后续立项的人知道缺口在哪，而不是把它们当成已验收结论：

- 声明最低版本 `0.1.5-rc.1` 的独立构建/运行复验：只确认了公开 stream API 存在、且 CI 在 rc.1 上跑 typecheck 与单测；浏览器回归只在 rc.2 上跑过。
- 剩余 HTTP 持续连接、请求耗时和补读次数的负载量测（ticket 01 最后一条验收）。
- 多页混合 Channel/Thread/Inbox 活跃负载：维护中的四页面回归以 Channel 为主。
- 断线期间提交、传输中断期间退出的浏览器组合验证：只覆盖了断网后无新提交的恢复。
- 草稿跨页覆盖、导航恢复、跨页私有已读、隐藏页面自动已读：本次未改变其语义，仍需各自的定向验证与决定；后台节流与跨页面连接共享不在范围内。

## 完成条件

1. 第一阶段：1、2、4 个同源页面均不再被 Team 更新通知占用普通 HTTP 连接而阻塞；实际操作、状态更新、取消和重连通过验证。 → **已满足**（浏览器回归 + Host/Client 单测，见「进展与结论」）。
2. 多页面状态清单中的草稿丢失、导航恢复、跨页未读和隐藏页自动已读都有已验证结论及明确处理决定；未解决的正确性问题不得以“后台优化”名义忽略，也不得宣称完整支持已完成。 → **未满足，按 Human 2026-09-17 指示明确关闭**；这些语义本次未被改动，也未被宣称解决（残余见上节）。
3. 确认受支持 Harness 版本上的生成、运行和浏览器行为；开发预览指向 `dsh web --profile web-dev`，不改稳定 profile。 → **部分满足**：rc.2 上已验证生成、运行和浏览器行为；rc.1 只有 CI 的 typecheck 与单测覆盖，未在独立环境重跑浏览器回归。
4. 更新受影响的正式文档、package README 和测试；未完成事项转为独立工作项或明确关闭后才归档。 → **已满足**：文档与 package README 双语已随 PR #25 更新；未完成事项已在上节明确关闭。

## 正式文档出口

以下正式文档已更新本次实现的行为：

- `docs/architecture.md` 及中文对应文档：通知传输、scope 语义、恢复与取消责任。
- `docs/development.md` 及中文对应文档：多页面浏览器回归入口与验证条件。
- Host / Client package README 双语文档：可观察的订阅契约、适用版本与多页面限制。
- 若改变草稿、导航或已读行为，更新其现有契约所属文档；不复制一份新的行为权威。

已按 `.scratch/AGENTS.md` 于 2026-09-17 移入 `archive/2026-09/`。原始日志和临时脚本留在忽略目录，未作为人工验收证据入库；验收数据以本文件与 ticket 01 的记录为准。
